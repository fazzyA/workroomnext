export const publicRoutes = [
    "/"
]

export const authRoutes = [
    "/login",
    "/signup"
]
/**
 * The routes that start with this are for  API authentication purpose
 * @type {string}
 */
export const apiAuthPrefix = "/api/auth"

export const DEFAULT_LOGIN_REDIRECT = "/settings"